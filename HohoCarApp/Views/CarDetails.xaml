<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="HohoCarApp.Views.CarDetails">
    <StackLayout Padding="10">
        <Label Text="{Binding SelectedCar.Brand}" FontSize="24" />
        <Label Text="{Binding SelectedCar.Model}" FontSize="20" />
        <Label Text="{Binding SelectedCar.Price, StringFormat='€{0:F2}'}" FontSize="18" />
        <Label Text="{Binding SelectedCar.Year}" FontSize="18" />
        <Label Text="{Binding SelectedCar.Description}" FontSize="16" />
    </StackLayout>
</ContentPage>
